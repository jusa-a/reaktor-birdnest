{
    "name": "server",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "node index.js",
        "dev": "nodemon index.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "rm -rf build && cd ../client && npm install && npm run build && cp -r build ../server"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "node-fetch": "^2.6.8",
        "xml2js": "^0.4.23"
    },
    "devDependencies": {
        "nodemon": "^2.0.20"
    }
}
